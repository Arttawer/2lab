#include <iostream>
#include "coordinates.h"

using namespace std;

void input(float &x, float &y, float &z)
{
	cin >> x;
	cin >> y;
	cin >> z;
}

int main()
{
	float x, y, z;
	input(x, y, z);
	coordinates point(x, y, z);
	//point.print();
	point 3 = point1 + point2;
}
